import React from 'react'
import { Route, Switch } from 'react-router'
import { App, Home, Article, Joke, Picture, Detail } from '../containers'

export default () => {
  const routes = (
    <div></div>
  )
  return routes
}
